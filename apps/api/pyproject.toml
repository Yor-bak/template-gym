[project]
name = "gym-api"
version = "0.1.0"
description = "Backend de Template-GYM (FastAPI + SQLAlchemy + PostgreSQL)"
requires-python = ">=3.12,<3.13"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.32",
  "sqlalchemy[asyncio]>=2.0",
  "asyncpg>=0.30",
  "alembic>=1.14",
  "pydantic[email]>=2.9",
  "pydantic-settings>=2.6",
  "python-jose[cryptography]>=3.3",
  "bcrypt>=4.2",
  "python-multipart>=0.0.12",
  # Cliente HTTP interno hacia admin-panel-j2ec (aprovisionamiento de
  # gimnasios, Decisión #10 de ese proyecto) — antes solo era dependencia de
  # test (para el TestClient de FastAPI), ahora también de runtime.
  "httpx>=0.27",
]

[dependency-groups]
dev = [
  "pytest>=8.3",
  "pytest-asyncio>=0.24",
  "ruff>=0.7",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 110
target-version = "py312"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
