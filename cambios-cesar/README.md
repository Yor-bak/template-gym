# Cambios — rama Cambios-cesar

Carpeta de trabajo para los cambios que se vayan definiendo sobre esta rama.
Cada punto que se acuerde va como su propio archivo o sección aquí, para
llevar registro de qué se pidió, qué se decidió y qué quedó hecho.

## Contexto de la rama (al momento de crear esta carpeta)

- Ya tiene fusionado el trabajo de la rama `QA` (fix de tipos, limpieza de la
  app legada de la raíz, performance de Historial de accesos).
- Después de esa fusión, el equipo:
  - Desconectó el dashboard/app de Supabase, volvió a modo mock local.
  - Arrancó un backend propio en FastAPI (`apps/api`), Fase 1: auth + members.
  - Rediseñó varias pantallas de la app móvil (login, activación, cliente).
  - Bajó el `minSdkVersion` del APK y arregló conflictos de puertos.

## Pendiente

_(se va llenando con los puntos que se definan)_
